import { RideVehiclesPipe } from './ride-vehicles.pipe';

describe('RideVehiclesPipe', () => {
  it('create an instance', () => {
    const pipe = new RideVehiclesPipe();
    expect(pipe).toBeTruthy();
  });
});
