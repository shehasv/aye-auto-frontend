import { FreeVehiclesPipe } from './free-vehicles.pipe';

describe('FreeVehiclesPipe', () => {
  it('create an instance', () => {
    const pipe = new FreeVehiclesPipe();
    expect(pipe).toBeTruthy();
  });
});
